export function publicReviewSummary(before, current, sensitivity) {
  const f = x => Number(x).toFixed(6);
  const range = x => `${f(x.low)}–${f(x.high)}`;
  const r = current.results;
  return `# Public research review — evidence correction

Application **0.7.2**, existing model **${current.modelVersion}**, snapshot **${current.datasetAsOf}**, data revision **public-review-2026-09-06**. Generated by 'npm run review:report' from separate before/current artifacts. [Canonical specification](MODEL_V7_SPEC.md).

## Decision and boundaries

**GO for an explicitly evidence-limited research preview of the comparison method. NO-GO for comprehensive factual risk monitoring, forecasting, measured production loss, or empirical company-capacity rankings.** This release must be read with its coverage exclusions. Only three records are factual-baseline eligible at the snapshot: VEU removal, the specified materials-control suspension, and the recovered Kawashiri component. Only the first two currently produce nonzero sources. Four occurrence claims are verified, including the BIS clarification retained as context; the remaining records are unresolved/context, not silently scored.

The repository was already public when checked through GitHub's API on September 6, 2026, contrary to the earlier private-source recommendation. No recognized repository license was reported. Proprietary notices and visibility were preserved. Ownership placeholders and commercial legal review remain owner decisions. Push builds now require a separate manual workflow input to deploy; this task does not deploy.

## Changes by type

- **Equations:** the bounded severity × exposure × persistence source and propagation operators remain. Component persistence extends the existing profile without changing global defaults. No v8 redesign or new model name. The application patch distinguishes corrections; the data revision and output digest distinguish changed results.
- **Assumptions:** coefficients describe comparative operational scope. Severity is ordinal intensity, exposure is assumed affected scope unless measured, and persistence evolves that effect. The 45-day market half-life and generic recovery defaults remain. Equal-split dependencies and facility ordinals remain explicit assumptions.
- **Data and interpretation:** narrower source scopes remove direct downstream consequences already propagated. DDR4/HBM is interpreted as commodity-memory availability pressure, not adverse HBM supplier performance or net revenue effects. Memory-allocation reports are grouped into one episode; unresolved reports stay in context.
- **Eligibility:** factual records require reviewed occurrence, explicit eligibility, source location and dated information. A URL and a high confidence label do not verify a claim. Confidence never scales impact. Original records are retained in SQLite and [the exported ledger](reference/event-evidence.json).

## Material record decisions

| Record | Decision and evidence |
| --- | --- |
| e1 | Quarantined. Exact July 3, 2026 rule not located in targeted BIS/Federal Register searches. [2024-28270, section C](https://www.federalregister.gov/documents/2024/12/05/2024-28270/foreign-produced-direct-product-rule-additions-and-refinements-to-controls-for-advanced-computing) already introduced HBM controls; it cannot verify a separate July 2026 rule. |
| p260807_man0807 | Quarantined. Original cited Tech Insider page could not be read; exact spot-price and allocation claims remain unresolved. Adverse HBM injection and direct downstream buyer injections removed from its illustrative model. |
| x2508_veurevoke | Verified and corrected using [FR 2025-16735](https://www.govinfo.gov/content/pkg/FR-2025-09-02/html/2025-16735.htm), summary/dates/background/removals. Three named authorisations removed, effective December 31, 2025. No universal shipment ban or measured global capacity loss inferred. Equipment procurement coefficients remain assumed. |
| h2607_kumamoto | Corrected to Kawashiri wafer input using [Renesas final update](https://www.renesas.com/en/about/newsroom/update-5-final-impact-2026-kumamoto-earthquake-renesas-operations). August 23 restoration is usable from August 24 publication. Finished output, shipments, backlog and other sites remain unverified. |
| h2606_subs | Verified context, excluded as an independent shock. [BIS May 31 guidance](https://www.bis.gov/media/documents/bis-guidance-may-31-2026.pdf), page 1, clarifies a preexisting requirement and qualifies ongoing compliant data-center use. It does not document a new physical supply cut. |
| h2510_truce | Verified specified suspension using [MOFCOM/GACC Announcement 70](https://www.mofcom.gov.cn/zwgk/zcfb/art/2025/art_b1ec77dd3f0d4762952904df7cdaadec.html). Materials-only direct assumed exposure; downstream effects propagate. |
| h2512_memory / h2603_memorypeak | Unresolved exact dated narratives, grouped with the DDR4 allocation episode to avoid independent shocks for repeated descriptions of the same shortage. |

Kawashiri's original day-7/60-day assumption retained 0.483333 on September 4. The corrected observed wafer-input residual is zero. Since the reviewed source for this component was published August 24, replay does not infer that its contents were known on July 28. [Milestone rules and limitations](RECOVERY_MILESTONES.md).

## Numerical reproduction

Before correction: **${f(before.curation.variants.baseline)}**. After correction: **${f(r.headlineIndex.base)}**. This is a **data correction**, not evidence of a decline in real-world risk. Removing unsupported incidents, narrowing direct sources and accounting for scoped recovery explains the movement. Missing coverage is not estimated as zero real-world disruption.

Original leave-one-incident-out differences, before any data correction:

| Primary incident | Baseline minus index without entire incident |
| --- | ---: |
${before.incidentInfluence.slice(0, 5).map(x => `| ${x.primaryId} | ${f(x.baselineMinusWithout)} |`).join('\n')}

These differences remove all updates/recoveries with the primary. They are **not additive contribution shares** because incidents overlap and aggregation saturates.

Original coupled exposure/profile range: **${range(before.curation.originallyTestedRange)}**. Original opposing adverse/mitigating settings: **${range(before.curation.opposingSignRange)}**. This reproduces the omitted-scenarios defect without forcing the corrected dataset to retain the old numbers.

| Current uncertainty class | Headline range | Interpretation |
| --- | --- | --- |
| Numerical parameters | ${range(sensitivity.numericalParameters.headlineEnvelope)} | 1,024-sample registry design, with raw Sobol diagnostics, bootstrap standard errors, convergence and seed replications |
| Model form | ${range(sensitivity.modelForms.headlineEnvelope)} | Tested categorical combinations, separate from continuous parameters |
| Curation | ${range(r.headlineIndex)} | ${r.headlineIndex.testedScenarioCount} tested scenarios: baseline, per-incident exposures, opposing signs, individual/combined profiles, shared groups and fixed-seed sampling |
| Data coverage | Not a scalar interval | Quarantined/missing incidents, unresolved denominators and sampled facilities |

None is a statistical confidence interval or a proven continuous bound, and the ranges are not added. Curation sampling uses a declared shared latent scope coordinate and opposing mitigating coordinates; it does not assume independently sampled incidents. Company criticality is structurally unaffected by event curation, not empirically validated by invariance. Its parameter sensitivity is reported separately, conditional on fixed company/network priors. Missing company denominators still prevent an empirical capacity-ranking claim.

Identical hypothetical severity-7 hazard screens against the two baselines:

| Radius scenario | Original index delta | Corrected index delta | Corrected headline with scenario |
| --- | ---: | ---: | ---: |
${before.hazardScenarios.map(x => `| ${x.id}, ${x.radiusKm} km | ${f(x.delta)} | ${f(r.scenarioDeltas[x.id].base)} | ${f(r.headlineIndex.base + r.scenarioDeltas[x.id].base)} |`).join('\n')}

Larger deltas can result from reduced baseline saturation. They are not evidence of greater measured damage. Scenarios use analyst ordinal site weights and a curated facility sample, not observed capacity.

## Denominators and site evidence

The original logic/AI total is 105.1% and memory-fabrication total 107.5%. The first mixes merchant accelerator and captive-program scopes; the second mixes DRAM and NAND denominators. A prior seed-only blend mixed quarterly company shares with annual market weights and never reached the canonical database. It was not adopted. Original coefficients are now explicitly retained as **illustrative assumptions**, with unknown period/denominator fields and transformation provenance. Normalization for a within-sample display does not repair these measurements. [612 numeric-input records](reference/MEASUREMENT-BASIS.md) expose the missing fields.

Company domicile is distinct from manufacturing location. Country-stage measurement bases remain unresolved where not documented. Facility scale is ordinal, and all site-to-site links are derived, not verified shipments. Scoped TSMC Fab 18 and ASML Veldhoven existence/function claims were checked against official sources in [facility evidence](reference/facility-evidence.json); current capacity, precise coordinates and links to individual customer sites remain unverified. Remaining 273 site claims were not individually reviewed in this pass.

The [complementary-input benchmark](COMPLEMENTARY_INPUT_BENCHMARK.md) shows equal incoming allocation diluting an essential missing input as more inputs are added. The physical toy bottleneck remains binding. This documents the heuristic's limitation without inventing precise edge coefficients or a capacity-constrained production model.

## Reproduction, documentation and preserved artifacts

Run 'node app/scripts/build-review-before.mjs' to reproduce revision 79289b1 in an isolated temporary extraction. Current sequence: 'npm run review:evidence', 'npm run snapshot', 'npm run curation', 'npm run sensitivity -- --samples 1024', 'npm run legacy', 'npm run demo', 'npm run review:report', 'npm run docs:generate' (all npm commands from app/). Evidence sync retains original records and decisions; exports use the canonical SQLite database.

Current experiments use the '-public-review.json' suffix. Frozen model benchmarks, existing benchmark JSON, archived specifications and briefing bodies remain unchanged. Original demo CSVs are retained in 'computation-demo/before-public-review-79289b1/'. The earlier synthetic-recovery experiment remains labeled pre-review, not relabeled as a current baseline result. Actual archived outputs differ from **current-model retrospective replay**. Genuine point-in-time validation requires dated network and input vintages, which this project does not have.

## Verification and remaining work

Executed checks and exact results are recorded in [the verification run](benchmarks/verification-run.json). The in-app browser connection was unavailable; browser results refer to the repository headless production smoke suite. Tests cover exclusion independent of confidence, source existence versus verification, dated component recovery, orphan updates, nonadditive incident removal, deterministic opposing-sign/profile combinations and explicit incompatible denominators.

**Public-preview blockers if claims are broadened:** restore/review missing material events before claiming comprehensive monitoring; establish common company denominators before claiming measured capacity rankings; obtain dated vintages before claiming point-in-time validation. Retain visible coverage and unresolved-source labels for the limited preview. Proprietary ownership placeholders and the public/private recommendation conflict remain unresolved owner matters, especially before commercial launch.

**Later research:** wider claim verification, measured site capacities, actual shipment relationships, empirically supported edge allocation, multi-site output/backlog recovery and external holdout calibration. These are disclosed limitations of a comparison prototype, not grounds for another wholesale redesign.
`;
}
